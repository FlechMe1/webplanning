%section.content
  .row.justify-content-center
    .col-md-6
      .box
        .box-body
          .alert.alert-info
            Avant de gérer une association merci de vous connecter

          = form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { :role => 'form'}) do |f|
            = devise_error_messages!
            .form-group
              %label Email
              = f.email_field :email, :autofocus => true, class: 'form-control'


            .form-group
              %label Mot de passe
              = f.password_field :password, class: 'form-control'
              - if devise_mapping.recoverable?
                = link_to new_password_path(resource_name), class: 'right' do
                  = fa_icon "question"
                  J'ai oublié mon mot de passe

            .form-actions
              %button.btn.btn-primary{type: :submit, class: "float-left"}
                Connexion

              - if devise_mapping.registerable? && controller_name != 'registrations'
                = link_to new_registration_path(resource_name), class: "btn btn-info float-right" do
                  Créer un compte

              .clearfix
              - if devise_mapping.rememberable?
                .checkbox
                  %label
                    = f.check_box :remember_me
                    Se rappeler de moi
